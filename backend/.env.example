PORT=4000
CONTACT_TO_EMAIL=takreem.jaffery@gmail.com

# Optional: fill these in to actually send emails via SMTP (e.g. Gmail app
# password, SendGrid, Mailgun, etc). Leave blank and submissions will just
# be logged to the server console instead — fine for local testing.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
